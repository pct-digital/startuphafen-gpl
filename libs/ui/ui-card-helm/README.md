# ui-card-helm

Vendored spartan-ng helm component (MIT, see libs/ui/LICENSE), adapted for the Startuphafen design system.

## Running unit tests

Run `nx test ui-card-helm` to execute the unit tests via [Jest](https://jestjs.io).
